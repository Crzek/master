import { Boxes } from "./Popstyle";

const Popap = (p) => {
    return (
        <Boxes display={p.display}>
            <h4>Felicidades Has Ganado!!!</h4>
        </Boxes>
    );
};

export default Popap;
